#include <stdio.h>
#include <stdlib.h>

#define LINES_DEFAULT 10

void tail(int lines);

int main(int argc, char* argv[]) {
    
    if(--argc > 1 && (*++argv)[0] == '-') {
        //Grab number of memes

    }

}
